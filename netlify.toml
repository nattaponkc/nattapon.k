[build]
  command = "cd frontend && npm run build"
  publish = "frontend/.next/standalone"
  functions = "backend"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
  NEXT_PUBLIC_API_URL = "https://nattapon.k/api"
